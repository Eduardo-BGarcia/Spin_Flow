import 'package:flutter/material.dart';

class ManutencaoAbas extends StatelessWidget {
  const ManutencaoAbas({super.key});

  @override
  Widget build(BuildContext context) {
    // Apenas um placeholder por enquanto
    return const Center(
      child: Text('Conteúdo da Aba Manutenção'),
    );
  }
}
